import { StyleSheet } from "react-native";

export const chessClockButtonStyles = StyleSheet.create({
    chessClockButton: {
        flex: 1,
        backgroundColor: "#e0e0e0",
        justifyContent: 'center',
        borderRadius: 20,
    },

    buttonText: {
        fontSize: 120,
        fontWeight: '700',
        textAlign: 'center'
    }
});
